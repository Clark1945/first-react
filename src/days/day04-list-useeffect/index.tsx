// Day 4 — List Rendering + useEffect
// TODO: array.map() + key(用唯一 id)、useEffect 依賴陣列、串接公開 API + Loading / Error
export default function Day04() {
  return (
    <section>
      <h1>Day 4 — List Rendering + useEffect</h1>
      <p>尚未開始 🚧</p>
    </section>
  )
}
